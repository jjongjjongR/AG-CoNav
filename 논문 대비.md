# 논문 초안 — 단일 공중 스캔 기반 플랫폼별 주행성 지도 생성과 이기종 지상 로봇 협업 항법

> **초안 v0.1 (2026-08-21).** AG-CoNav 저장소의 실측 기록(`docs/1`~`docs/14`)을
> 논문 형식으로 재구성한 것이다. **측정된 값과 아직 측정하지 않은 계획을
> 명시적으로 구분**했다 — 계획 항목에는 〔예정〕 표시를 달았다.

---

## 초록 (Abstract)

미지의 도시 환경에서 지상 로봇이 목표까지 이동하려면 먼저 지형을 알아야
한다. 기존 LiDAR-관성 SLAM(LIO-SAM, FAST-LIO2, GLIM)은 로봇이 **직접 지나간
곳만** 지도로 만들 수 있어, 이동 전에는 어디로 갈 수 있는지 알 수 없다.
본 연구는 드론이 먼저 상공에서 지형을 훑어 **지상 로봇이 출발하기 전에 이미
완성된 사전 지도**를 제공하고, 그 한 번의 스캔에서 **플랫폼별로 서로 다른
주행성 지도**를 분리 생성하는 통합 시스템을 제시한다.

핵심은 주행성 기준이 로봇마다 다르다는 점이다. 본 연구는 4륜 로봇(Husky
A300)과 4족 로봇(Unitree Go2)의 등판각·단차 한계를 시뮬레이션에서 직접
측정해(각 3회 반복) 확정하고(wheel 10°/0.08 m, leg 15°/0.15 m), 동일한
드론 고도 지도에서 두 개의 주행성 지도를 갈라냈다. 서울 성동구를 재현한
578 × 486 m 도시 환경에서 드론·4륜·4족 3대를 하나의 시뮬레이션으로 구동해,
사람의 개입 없이 단일 launch 로 **지도 생성 → 주행성 분리 → 위치추정 →
자율주행 → 지상 지도 누적 → 병합**의 전 과정을 통과시켰다.

---

## 1. 서론 (Introduction)

### 1.1 문제

재난 현장이나 미지의 도시 환경에 투입된 지상 로봇은 두 가지를 동시에
해결해야 한다. **어디를 지날 수 있는가**(주행성)와 **지금 어디에 있는가**
(위치추정)다. 기존 접근은 대부분 후자를 풀고 전자를 부산물로 얻는다 —
로봇이 이동하면서 SLAM 으로 지도를 만들고, 그 지도로 다음 경로를 정한다.

이 구조에는 본질적 한계가 있다. **로봇이 가 보기 전에는 그곳을 알 수 없다.**
막다른 길에 들어가 되돌아 나오는 비용을 사후에야 알게 되고, 좁은 도시
블록에서는 이 비용이 전체 임무 시간을 지배한다.

두 번째 한계는 덜 지적된다. **주행성은 로봇마다 다르다.** 같은 15° 경사가
4족에는 통과 가능하고 4륜에는 불가능하다. 그런데 대부분의 협업 항법 연구는
지도를 **하나만** 만든다. 이기종 로봇을 함께 운용하면서 지도는 공유하는
순간, 가장 성능이 낮은 플랫폼 기준으로 맞추거나(보수적) 사고를 감수하거나
(낙관적) 둘 중 하나를 택하게 된다.

### 1.2 접근

본 연구는 **공중 사전 스캔**으로 첫 번째 한계를, **플랫폼별 주행성 분리**로
두 번째 한계를 다룬다.

드론이 84 m 상공에서 하향 LiDAR 로 대상 지역 전체를 훑어 2.5D 고도 지도를
만든다. 이 지도는 지상 로봇이 **한 발짝도 움직이기 전에** 완성된다. 이어서
같은 고도 지도에서 셀별 경사와 단차를 계산하고, 로봇별 통과 기준을 적용해
`wheel용`·`leg용` 두 장의 주행성 지도를 분리한다. 지상 로봇들은 각자의
지도로 Nav2 경로를 계획해 이동하면서 자기 LiDAR 로 근거리 지형을 다시
측정하고, 마지막에 세 지도를 하나로 병합한다.

### 1.3 기여

1. **단일 공중 스캔에서 플랫폼별 주행성 지도를 분리 생성**하는 파이프라인.
   기준값은 문헌 인용이 아니라 각 로봇을 계단식 램프·단차 하네스에서
   직접 주행시켜 3회 반복 측정으로 확정했다(§4.2).
2. **드론 스캔 조건(고도·속도·간격)이 최종 주행 성공을 좌우함을 실측**.
   같은 파이프라인에서 순항 속도만 10 → 6 m/s 로 바꾸자 wheel 의 경로계획
   실패가 19건 → 0건으로, 주행 거리가 18.5 m → 191.5 m 로 바뀌었다(§4.1).
3. **이기종 3로봇 통합 시뮬레이션의 종단 검증**. 단일 launch, 사람 개입
   없음, 6개 모듈 전체 통과를 실행 로그로 제시한다(§5).
4. 재현 가능한 평가 지표와 도구 공개 — 자유 공간을 로봇 반지름 원판으로
   침식한 뒤의 **최대 연결덩어리 비율**(§3.4).

---

## 2. 관련 연구 (Related Work)

### 2.1 LiDAR-관성 오도메트리·SLAM

**LIO-SAM** 은 스캔 매칭과 IMU 예비적분을 인자 그래프로 묶어 실시간
오도메트리와 루프 클로저를 제공한다. **FAST-LIO2** 는 반복 확장 칼만
필터와 증분 kd-tree(ikd-Tree)로 특징 추출 없이 원시 점군을 직접 정합해
계산량을 크게 줄였다. **GLIM** 은 GPU 가속 복셀 기반 정합과 전역 인자
그래프 최적화로 정확도와 강건성을 함께 높였다.

세 방법 모두 **로봇이 지나간 궤적 주변**을 지도로 만든다. 본 연구의
비교 대상으로 삼되, 경쟁 관계가 아니라 **상보 관계**로 본다 — 이들은
근거리 정밀도가 높고, 공중 사전 지도는 미방문 영역을 덮는다.

### 2.2 공중-지상 협업

드론이 지상 로봇의 경로를 안내하는 연구는 다수 있으나, 대부분 **단일 지도**
를 산출하거나 **단일 지상 플랫폼**을 전제한다. 본 연구는 하나의 스캔에서
이기종 플랫폼별 지도를 분리한다는 점에서 구별된다.

### 2.3 주행성 분석

경사·단차·거칠기로 통과 가능성을 판정하는 방식은 표준적이다. 본 연구의
차이는 **임계값을 문헌에서 가져오지 않고 대상 로봇으로 직접 측정**했다는
점과, 그 임계값이 **컨트롤러에 종속**됨을 명시적으로 다룬 점이다(§6.1).

---

## 3. 시스템 (Methods)

### 3.1 구성

| 로봇 | 모델 | 센서 | 역할 |
|---|---|---|---|
| 드론 | Gazebo 멀티콥터 (kinematic pose 제어) | Ouster OS1-32 하향 | 사전 지도 |
| 4륜 | Clearpath Husky A300 | OS1-32 | 개활지·연속 도로 |
| 4족 | Unitree Go2 (`rl_quadruped_controller`) | OS1-32 | 낮은 장애물 구간 |

ROS 2 Jazzy · Gazebo Harmonic · Nav2 · `grid_map` · `robot_localization`.
전 지도 해상도 0.10 m/cell, 전역 프레임 `map` 단일, 미관측 셀 = NaN.

### 3.2 모듈 파이프라인

```
A 드론 2.5D 지도  →  F 주행성 분리  →  B 위치추정  →  C Nav2 주행
                          │                              │
                          └─ wheel용 / leg용             D 지상 지도 누적
                                                          │
                                                          ▼
                                                       E 병합
```

**단계 게이트.** `pipeline_stage_gate` 가 A→F 완료를 확인한 뒤에만 B 이후를
연다. 지상 로봇은 시뮬레이션 시작부터 월드에 존재하지만, 드론 스캔이 끝날
때까지 **지도를 누적하지 않는다**(`mapping_active=False`). 이동 직전부터
누적을 시작해 그 결과가 E 에서 병합된다.

### 3.3 플랫폼별 주행성 판정 (모듈 F)

셀별로 경사와 단차를 계산하고 로봇별 기준으로 이진 판정한다.

경사 기저는 `slope_window × resolution × 2` 로 정한다. 이 값이 결과를
좌우한다 — 실측으로 진짜 0.2~1.5° 인 지형에서 `slope_window=1` 은 5.96° 를,
`slope_window=3` 은 1.76° 를 냈다. 0.1 m 격자에서 창을 1로 두면 인접 셀
높이 잡음이 그대로 경사로 증폭되기 때문이다. **확정값 3.**

### 3.4 평가 지표

지도 품질을 셀 정확도로만 재면 실제 주행 가능성을 반영하지 못한다. 자유
공간이 아무리 많아도 **잘게 쪼개져 있으면** 목표까지 갈 수 없기 때문이다.

본 연구는 자유 셀을 로봇 반지름(wheel 0.55 m, leg 0.40 m) 원판으로 침식한
뒤 **가장 큰 연결 성분이 평가 영역에서 차지하는 비율**을 주 지표로 쓴다
(`eval_connectivity.py`). FN(미검출)은 순위를 매기는 데는 쓰되 크기를
재는 데는 쓰지 않는다.

---

## 4. 파라미터 확정 실험 (Parameter Studies)

### 4.1 드론 스캔 조건

| 파라미터 | 확정값 | 근거 |
|---|---|---|
| 고도 | **84 m** | OS1-32 의 10% 반사율 사거리 90 m 에서 가장자리 슬랜트 거리를 감당하는 상한 |
| 순항 속도 | **6 m/s** | 아래 표 |
| 스트립 간격 | **3 m** | 자유 공간 단일성 **99.1%** (2.5 m 36.1%, 4 m 37.8%) |

간격은 자유 공간의 *양*이 아니라 그것이 *하나로 이어지는지*로 갈렸다.
2.5 m 는 침식 후 자유 공간이 41.10% 로 3 m(41.06%)와 사실상 같은데 최대
연결덩어리는 14.85% vs **40.71%** 다 — 임계점 근처라 작은 차이로 전역
연결이 뒤집힌다. 2 m 는 단일성 99.9% 로 더 낫지만 +2.6시간에 +5.72 %p
(1,636 초/%p)라 한계비용이 맞지 않는다.

**속도가 지도 품질을 지배한다.** 같은 파이프라인을 조건만 바꿔 두 번 돌린
결과다.

| 조건 | wheel 자유 | wheel 점유 | 높이 오차 σ | 경로계획 실패 | 주행 결과 |
|---|---|---|---|---|---|
| v10 × 6 m | 37.5% | 27.7% | 0.536 m | **19건** | 18.5 m 후 ABORTED |
| **v6 × 3 m** | **88.5%** | 10.1% | **0.017 m** | **0건** | **191.5 m SUCCEEDED** |

높이 오차 표준편차가 **31배** 차이 난다. 6 → 8 m/s 구간에서 기체 제어가
무너지는 것이 원인이다 — 선 이탈 **0.053 → 0.312 m**(6배), 고도 오차
**0.610 → 2.999 m**(5배). 더 올려도 나아지지 않는다(v10 0.580/2.752,
v12 0.953/2.585). 고도가 3 m 씩 흔들리면 그대로 높이 잡음이 되어 주행성 지도가
벌집이 된다. 제동 거리도 100 m 스트립에서 6 m/s 는 33%, 10 m/s 는 57% 를
차지해 순항 구간 자체가 줄어든다.

### 4.2 로봇별 지형 통과 한계

계단식 램프(각도 구간을 이어 붙인 월드)와 단차 하네스에서 **각 조건 3회
반복** 측정했다.

**4족 컨트롤러 비교**

| 컨트롤러 | 등판 한계 | 실패 양상 | 단차 한계 | 종단 주행 |
|---|---|---|---|---|
| `unitree_guide` | **20°** | 25°에서 **전복** (이탈 1.51~3.54 m) | 150 mm (`gait_height 0.20`) | **실패 (전복)** |
| **`rl_quadruped` (robot_lab)** | 15° | 20°에서 **정지** (이탈 0.13~0.62 m) | 150 mm (기본값) | **성공** |
| CHAMP | 측정 중 | — | — | — |
| legged_gym / himloco 정책 | 평지 보행 실패 | — | — | — |

등판만 보면 `unitree_guide` 가 5° 우세하나, Nav2 자율주행에서 반복 전복해
운용에서 제외했다(§6.1). **운용 컨트롤러는 `rl_quadruped`.**

**4륜 (Husky A300)**

| 항목 | 결과 |
|---|---|
| 등판 | 10° 통과 3/3, 12° 정지 3/3 |
| 단차 × 속도 | 80 mm: 0.5·0.8 실패 / 1.2·1.6 통과 2/2 · 100 mm: 전 속도 실패 |
| 최적 속도 | **1.2** (실측 0.553~0.555 m/s, 이탈 0.22~0.39 m). 1.6 은 더 빠르나 이탈 0.65~0.95 m |

**확정 임계값**

| | 최대 경사 | 최대 단차 |
|---|---|---|
| wheel | **10.0°** | **0.08 m** |
| leg | **15.0°** | **0.15 m** |

> leg 값은 운용 컨트롤러(`rl_quadruped`) 기준이다. 컨트롤러를 바꾸면 이
> 값의 근거가 사라진다. 성동구 맵에서는 20° → 15° 하향의 실비용이 0 으로
> 측정됐다 — 도심 지형은 지면 6° 이하와 거의 수직인 건물 벽으로 양분되어
> 15~20° 대역 셀이 존재하지 않는다.

---

## 5. 종단 검증 (End-to-End Verification)

### 5.1 조건

서울 성동구 재현 월드(578 × 486 m), **단일 `agconav_all.launch.py`**,
GUI 없음, **사람 개입 없음**. 드론 실비행(모듈 A)부터 병합(E)까지 한 번에
실행했다. 스폰 (−195.7, 76.4) → 목표 wheel (−67.4, −60.9) · leg
(−65.0, −62.4), 직선거리 약 191 m.

### 5.2 결과 (2026-08-21, 전체 소요 331분)

| 모듈 | 결과 |
|---|---|
| A 드론 스캔 | 320 웨이포인트 / **92.9 km** 완주, 선 이탈 **0.00~0.06 m**, 320분 |
| A 고도 지도 | 5786 × 4855 셀, 108 MB |
| F 주행성 분리 | wheel 통과 24,106,640 / leg 통과 24,615,099 셀 |
| B 위치추정 | EKF + navsat 정상 |
| C Nav2 | wheel **Goal succeeded** (494 s) · leg **Goal succeeded** (308 s) |
| D 지상 누적 | wheel 1,280,385 셀 · leg 1,153,068 셀 |
| E 병합 | 5786 × 4855 셀 (578.6 × 485.5 m), 108 MB |

실패 신호 0건 — `Goal ABORTED` 0, `Resulting plan has 0 poses` 0,
**경로계획 실패 0**, 예외 0, 프로세스 사망 0.

비행 품질이 확정 조건과 일치했다. 선 이탈 0.00~0.06 m 는 docs/9 가 v6 에서
측정한 0.053 m 와 같은 수준이고, v8 의 0.312 m 와는 확연히 다르다.
F 판정 결과도 별도 실행의 재생 지도와 **0.003% 이내로 일치**해 확정 조건
(84 m / 6 m/s / 3 m)의 재현성이 확인됐다.

### 5.3 시뮬레이션 합성물이 지도를 오염시킨다 — 방법론적 주의

첫 실행은 **경로계획 76회 연속 실패**로 두 로봇이 출발조차 못 했다.
원인은 목표 좌표에 놓아둔 **시각용 표식**(반지름 0.4 m, 높이 3 m 원기둥)
이었다. 충돌체가 없어도 **GPU LiDAR 는 visual 을 때리므로**, 드론이 84 m
상공에서 이를 지형으로 관측해 목표를 막았다.

| 조건 | wheel 목표 반경 1 m 자유 | leg |
|---|---|---|
| 표식 있음 | 27.9% | 31.3% |
| `visibility_flags` GUI 전용 시도 | 28.6% | 27.2% (효과 없음) |
| **표식 제거** | **100.0%** | **100.0%** |

시뮬레이션 실험에서 시각화용으로 월드에 넣은 객체는 센서 데이터에
그대로 들어간다. 목표·경로·주석 표식은 렌더링 계층(RViz)에 두어야 하며,
월드에 두면 지도가 오염된다. **`visibility_flags` 로는 분리되지 않는다.**

### 5.4 지상 관측의 이상치 처리

지상 로봇의 고도 오차는 **사거리에 비례**한다. OS1 은 170 m 까지 재고
수직 −21.2° 까지 내려다보므로 원거리 하향 빔은 센서보다 최대 61.5 m
아래에 찍히고, 여기에 보행 중 자세 오차가 겹치면 증폭된다(170 m 에서
2° = 5.9 m).

| 경로로부터의 거리 | leg: \|오차\|>3 m 비율 |
|---|---|
| 0~10 m | 59.8% |
| 80~160 m | **88.7%** |

걷지 않는 wheel 은 −5 m 아래 셀이 0.085% 인 반면 leg 는 **5.1%**
(65,062셀), 최저 −46.4 m 였다. 해당 구간의 정답 지형은 2.0~6.3 m 다.

두 겹으로 걸렀다. (1) 지상 매핑 사거리 **60 m** 제한 — 그 바깥은 드론
지도가 98.9% 덮으므로 손실이 없다. (2) 센서 기준 z 밴드 **−12 ~ +60 m**.

**효과 (실측 대조)**

| | 최저 z | −5 m 아래 비율 |
|---|---|---|
| wheel 이전 → 이후 | −9.9 → **−3.7 m** | 0.085% → **0.000%** |
| leg 이전 → 이후 | −46.4 → **−12.0 m** | 5.099% → **2.363%** |

wheel 은 완전히 사라졌고 leg 는 절반 이하가 됐다. **완전히 없어지지는
않았다** — 남은 값이 밴드 하한에 붙어 있어, 60 m 안에서도 보행 자세
오차가 여전히 기여한다. 더 줄이려면 관측 범위를 더 잃어야 한다.
주행 중 기각률은 wheel 기준 매 스캔 27,307개 중 **2,927개(10.7%)**,
최대 169.9 m 반사까지 포함했다. leg 격자는 4008×4613 → **2555×2452**
로 3.1배 줄었다 — 원거리 오측이 격자를 부풀리던 것이 사라졌다.

## 6. 한계점 (Limitations)

### 6.1 등판 성능이 더 높은 컨트롤러를 쓰지 못했다

`unitree_guide` 는 램프 하네스에서 20° 를 통과했다(RL 은 15°). 그러나
**Nav2 자율주행에서는 반복해서 전복**했다 — roll 78° → 140° → −53°.

원인을 좁히는 과정에서 다음을 배제했다.

- **지형이 아니다.** 정답 heightmap 으로 경로 191 m 를 400점 샘플링한 결과
  최대 경사 **5.2°**, 최대 단차 **12 mm**. guide 한계의 1/4 이하다.
- **파이프라인·지도·Nav2 설정이 아니다.** 컨트롤러만 RL 로 바꾸고 나머지를
  그대로 두자 목표에 도착했다.
- **명령 끊김이 아니다.** `/leg/cmd_vel` 19.6 Hz, 컨트롤러 200 Hz — 명령
  하나당 약 5 주기가 확보된다.
- **RTF 저하 자체가 아니다.** `use_sim_time` 에서 컨트롤러는 시뮬 시간
  기준 200 Hz 를 그대로 받는다.

시도한 대책: `gait_height` 0.08 → 0.20(단차는 해결, 전복은 그대로),
Nav2 한계를 guide 설계값에 정합(지표는 개선, 전복은 그대로).

**남은 가설(미검증).** `StateTrotting` 이 Nav2 처럼 선속도와 각속도가
동시에 계속 바뀌는 입력을 전제하지 않은 것으로 보인다. 요 명령에만 0.9
저역통과가 걸려 있고 선속도에는 필터가 없으며, 목표 속도를 현재 속도
±0.2 로, 위치 명령을 현재 위치 ±0.05 로 포화시키는 두 제약이 빠른 명령
변화에서 서로 상쇄될 수 있다. **후속 연구로 남긴다.**

이 제약의 대가는 **등판 한계 5° 손실**이다. 다만 §4.2 에서 밝혔듯 성동구
맵에서는 실비용이 0 이었고, 자연 지형 월드에서는 다시 측정해야 한다.

### 6.2 드론 위치는 정답(GT)을 쓴다

드론은 kinematic 제어이고 `map → drone/base_link` TF 를 Gazebo
`OdometryPublisher` 에서 그대로 받는다. 즉 **드론 지도는 위치추정 오차 0
에서 만들어진다.** 지상 로봇은 GPS+IMU EKF 로 자기 위치를 추정하므로
이 가정을 쓰지 않는다.

이는 비교 실험 설계에 직접 영향을 준다(§7). 본 연구의 기여는 위치추정이
아니라 사전 주행성 지도이므로, 비교 대상에도 동일하게 GT 를 제공해
위치추정 오차를 교란 변수에서 제거하는 것이 타당하다.

### 6.3 계산 부하

`gz sim` 서버가 단일 스레드로 CPU 93% 를 쓴다. 3대의 LiDAR(드론 OS1
8,192 ray, Go2 18,000, velodyne 7,040)가 같은 스레드에서 10 Hz 로
렌더링되기 때문이다.

다만 부하는 "전부 한 시뮬"인지가 아니라 **어느 모듈이 돌고 있는지**로
갈린다. 이번 통합 실행에서 실측한 값이다.

| 구간 | RTF | 비고 |
|---|---|---|
| 모듈 A (지상 로봇은 대기) | **1.08** | 센서는 3대 다 돌지만 Nav2 는 아직 없다 |
| 모듈 C~D (Nav2 2세트 + 지상 매핑) | 0.46 | load 38~47 로 급등 |

즉 병목은 LiDAR 렌더링만이 아니라 **Nav2 스택 2세트**다. 전체 맵 스캔이
92.9 km(6 m/s 기준 시뮬 4.3시간)라 통합 실행의 벽시계 소요는 여전히
크다 — 이번 실행은 총 **331분**이 걸렸다.

### 6.4 그 밖

- 단일 월드(서울 성동구 재현)에서만 검증했다. 자연 지형·계단·실내는 범위 밖.
- 드론은 자율비행이 아니라 사전 지정 경로 재생이다.
- 동적 장애물(보행자·차량)은 다루지 않는다.

---

## 7. 비교 실험 설계 〔예정〕

### 7.1 비교 대상

| 방법 | 종류 | 입력 |
|---|---|---|
| **AG-CoNav (본 연구)** | 공중 사전 지도 + 플랫폼별 주행성 | 드론 하향 LiDAR |
| GLIM | 지상 LiDAR-관성 SLAM | 지상 LiDAR + IMU |
| FAST-LIO2 | 지상 LiDAR-관성 오도메트리 | 지상 LiDAR + IMU |
| LIO-SAM | 지상 LiDAR-관성 SLAM | 지상 LiDAR + IMU |

### 7.2 GT pose 취급 — 두 역할을 분리한다

**(a) 평가 기준으로서의 GT.** 네 방법 모두 동일한 정답 heightmap 에 대해
동일한 도구(§3.4)로 평가한다. **선택의 여지가 없다.**

**(b) 입력 pose 로서의 GT.** 본 연구의 드론은 GT pose 를 쓴다(§6.2).
비교 대상만 자기 오도메트리를 쓰게 하면 **그들의 드리프트를 매핑 성능으로
오인**하게 된다. 따라서 GLIM·FAST-LIO2·LIO-SAM 에도 GT 궤적을 주어
mapping-only 모드로 돌린다.

이 조정은 **비교 대상을 유리하게** 만드는 방향이다(드리프트 제거). 그
상태에서도 본 연구가 우위를 보이면 주장이 훨씬 강해진다.

**두 조건을 모두 싣는다.**

| 조건 | 비교군 | 본 연구 | 읽는 법 |
|---|---|---|---|
| 1 | GT 궤적, mapping-only | 현행 | 매핑 접근의 순수 비교 (주 결과) |
| 2 | 자기 추정 pose (정상 SLAM) | 현행 | 드리프트의 실제 대가 (부가) |

### 7.3 지표 — 지도 정확도만으로 붙지 않는다

지상 LiDAR 는 근거리라 84 m 상공 스캔보다 국소 정확도가 높은 것이 당연하다.
본 연구의 주장은 "지도가 더 정확하다"가 아니라 **"지상 로봇이 움직이기
전에 이미 지도가 있다"**는 선험성이다. 따라서 다음을 함께 보고한다.

1. **t=0 시점에 계획 가능한 영역 비율** — 이동 없이 경로를 낼 수 있는 범위.
2. **목표 도달까지의 총 시간·경로 길이** — SLAM 계열은 탐색 시간이 더해진다.
3. **최대 연결덩어리 비율**(§3.4) — 지도 자체의 주행 가능성.
4. 높이 오차 RMSE / σ — 참고용 (건물 셀은 정답 heightmap 에 없으므로 제외).

---

## 8. 결론 (Conclusion)

본 연구는 드론의 단일 공중 스캔에서 이기종 지상 로봇 각각을 위한 주행성
지도를 분리 생성하고, 그것으로 지상 로봇이 **탐색 없이** 목표까지 이동하는
통합 시스템을 구현하고 검증했다.

세 가지를 확인했다. 첫째, **드론 스캔 조건이 최종 주행 성공을 좌우한다** —
순항 속도 하나(10 → 6 m/s)로 경로계획 실패가 19건에서 0건으로, 주행
거리가 18.5 m 에서 191.5 m 로 바뀌었다. 둘째, **주행성 임계값은 컨트롤러에
종속된다** — 같은 로봇이 컨트롤러에 따라 20° 와 15° 로 갈렸다. 셋째,
**이기종 3로봇 통합 파이프라인이 사람 개입 없이 단일 launch 로 동작한다** —
6개 모듈 전 구간, 실패 신호 0건.

한계도 분명하다. 등판 성능이 더 높은 컨트롤러를 자율주행 불안정 때문에
쓰지 못했고(§6.1), 드론 위치추정은 정답을 가정하며(§6.2), 단일 월드에서만
검증했다. 기존 SLAM 계열과의 정량 비교(§7)는 설계를 마쳤으나 아직
수행하지 않았다. 이상치도 완전히 제거하지는 못했다(§5.4).

---

## 부록 A. 실험 문서 대응표

| 절 | 근거 문서 |
|---|---|
| §3.4 평가 지표 | `docs/README.md` |
| §4.1 드론 속도 | `docs/4`, `docs/9`, `docs/10` |
| §4.1 스트립 간격 | `docs/10` §간격 비교, `docs/10~11` |
| §4.1 센서 조합 | `docs/2` (1024×10 유지 결론) |
| §4.2 4족 컨트롤러 | `docs/11`, `docs/13` |
| §4.2 4륜 한계 | `docs/12` |
| §5 종단 검증 | `docs/10`, `docs/14` |
| §6.1 guide 한계 | `docs/14` |

## 부록 B. 재현

```bash
# 전체 파이프라인 (A -> F -> B -> C -> D -> E)
ros2 launch agconav_bringup agconav_all.launch.py \
    use_nav2:=true headless:=true enable_drone:=true spawn_ground_early:=true

# 평가
python3 src/agconav_traversability/scripts/eval_connectivity.py <map>
```

확정값은 각 설정 파일에 잠금 주석으로 표시되어 있다 —
`scan_path_fullmap.yaml`(고도 84 m·간격 3 m), `agconav_all.launch.py`
(순항 6 m/s·컨트롤러 rl), `traversability_{wheel,leg}.yaml`(임계값),
`terrain_feature_calculator.yaml`(`slope_window` 3).
